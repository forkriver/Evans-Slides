jQuery(document).ready(function($) {
	$('.carousel').slick({
	  infinite: true,
	  dots: true,
	  arrows: true,
	  autoplay: true,
	  centerPadding: '30px',
	  centerMode: true,
	  lazyLoad: 'ondemand'
	});
});

